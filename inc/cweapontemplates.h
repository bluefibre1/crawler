#pragma once

#include "cweapontemplate.h"

class WeaponTemplates
{
public:

    static const WeaponTemplate& SWORD();
    static const WeaponTemplate& CLAW();
    static const WeaponTemplate& PUNCH();
    static const WeaponTemplate& FIRE();
    static const WeaponTemplate& SPELL();
    static const WeaponTemplate& SNOWBALL();

};
