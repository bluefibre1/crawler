#pragma once


enum class Color
{
    BLACK = 0,
        RED,
        GREEN,
        YELLOW,
        BLUE,
        MAGENTA,
        CYAN,
        WHITE,
        INVALID
        };

enum class ColorStyle
{
    NORMAL=0,
        BRIGHT,
        DIM,
        INVALID
        };
